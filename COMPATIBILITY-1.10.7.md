# تقرير التوافق — دارجات 1.10.7

## خلاصة دقيقة

التطبيق مبني كواجهة ويب عربية RTL متجاوبة، والتوافق البرمجي المتوقع جيد مع المتصفحات الحديثة وWebView الحديث على الهواتف والأجهزة اللوحية وأجهزة Windows. لا يصح اعتبار ذلك إثباتاً لتوافق مطلق مع كل طراز أو إصدار نظام؛ بعض النتائج، خصوصاً الطباعة الأصلية والحفظ الأصلي وWebView القديم، تحتاج اختباراً على الجهاز نفسه.

## ما تم التحقق منه في المصدر

- وجود `meta viewport` مع `viewport-fit=cover` ودعم التكبير.
- اتجاه الصفحة العربية `dir="rtl"`.
- قواعد CSS للشاشات الصغيرة حتى 380px، والهواتف حتى 600px، والأجهزة المتوسطة حتى 1100px، والشاشات المكتبية.
- قواعد منفصلة للاتجاه العمودي والأفقي، بما فيها الشاشات الأفقية ذات الارتفاع القصير.
- منع overflow الأفقي العام، مع جعل الجداول وأشرطة الأدوات القابلة للتمرير أفقياً عند الحاجة.
- تقليل أحجام وعروض عناصر الشريط العلوي، وتحويله إلى شريط قابل للتمرير بدلاً من قص الأزرار.
- إبقاء قواعد الطباعة وقوالبها خارج طبقة الاستجابة الخاصة بالشاشة؛ لم يتم تغيير مقاسات الورق أو الهوامش أو الاتجاهات أو ترتيب عناصر الطباعة.
- فحص بناء JavaScript لكل كتل الصفحة الداخلية، وفحص PDF المتجهي العربي، وفحص سلامة الحزمة المضغوطة.

## مصفوفة التوافق المتوقع

| البيئة | التوقع البرمجي | ما يحتاج جهازاً فعلياً |
|---|---|---|
| Android Chrome / Samsung Internet الحديث | جيد | الطباعة، مشاركة الملفات، أداء WebView، حفظ الملف فعلياً |
| Samsung الحديثة | جيد عبر المتصفح أو WebView الحديث | اختبار طرازات وإصدارات One UI المختلفة |
| Huawei وHonor الحديثة | جيد عبر المتصفح/WebView المتاح | اختلاف Huawei WebView أو المتصفح، مشاركة/حفظ الملفات، الطباعة |
| أجهزة Android اللوحية | جيد مع التكيف للحجم والاتجاهين | تعدد النوافذ، لوحة المفاتيح، الطباعة، كثافة الشاشة |
| Windows Chrome / Edge / Firefox الحديث | جيد | اختيار مكان الحفظ، الطابعات الفعلية، اختلاف DPI |
| WebView أو متصفح قديم | غير مضمون | دعم Web API وCSS وPDF والطباعة حسب الإصدار |

## حدود التأكيد

لم يتم تشغيل اختبارات على جهاز Samsung أو Huawei أو Honor فعلي، ولا على Android System Print أو MediaStore أو تطبيق Files داخل APK في هذه البيئة. لذلك لا يتم الادعاء بأن كل هذه الوظائف تعمل فعلياً على كل جهاز. عند عدم وجود Native bridge، تبقى وظائف الحفظ والطباعة ضمن قدرات المتصفح/WebView وقد تعود إلى تنزيل المتصفح أو المشاركة القياسية.

## التحقق بعد التحديث

- الإصدار: `1.10.7`
- تمت إضافة زر **«وقت الفترة التجريبية»** داخل «حول التطبيق».
- الزر يعرض المدة المتبقية أو حالة الاشتراك، ويفتح لوحة التفعيل الموجودة مسبقاً عبر `window.__actOpen` دون إعادة بناء منطق التفعيل.
- لم يتم تغيير Firebase أو بوابة التفعيل أو البيانات المحلية أو APK wrapper أو تخطيطات الطباعة وPDF.
